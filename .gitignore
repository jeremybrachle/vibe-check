__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.venv/
venv/
.env
vibe_check.db
*.log
# Accumulated runtime data — not secrets but shouldn't live in the repo
data/*.jsonl

# Certs / keys
*.pem

# Internal planning / AI handoff docs — ignore every .md except READMEs
# (at any depth). Drop a new HANDOFF_*.md, AWS_*.md, etc. anywhere and
# it'll be auto-hidden from git.
*.md
!README.md
!CHANGELOG.md
!**/README.md